// 对于数据的处理，以闭包形式保存数据
// 借用 JQ 的模式，来处理普通对象和DOM对象的不同处理

N.define( "data", function(){
    var cache = [],
        uid = 0,
        NID = N.NID;

    /** 
    */
    function date( obj, key, value ){

    }
} );